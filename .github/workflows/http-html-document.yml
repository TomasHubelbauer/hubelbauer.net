name: http-html-document
on:
  push:
  schedule:
    - cron: '0 * * * *'
jobs:
  check-dns:
    runs-on: ubuntu-latest
    steps:
      - name: Check hubelbauer.net:80 HTML document
        run: ./http-html-document.sh
