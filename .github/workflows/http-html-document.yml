name: http-html-document
on:
  push:
  schedule:
    - cron: '0 * * * *'
jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Check hubelbauer.net:80 HTML document
        run: .github/workflows/http-html-document.sh
