name: dns-a-records
on:
  push:
  schedule:
    - cron: '0 * * * *'
jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Check hubelbauer.net DNS A records
        run: .github/workflows/dns-a-records.sh
